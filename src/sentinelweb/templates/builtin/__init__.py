"""Bundled YAML detection templates.

This is a regular package (not a namespace package) so
``importlib.resources.files("sentinelweb.templates.builtin")`` works
reliably across all installation methods supported by Python 3.11+,
including wheel, sdist, editable, and zip-imported.
"""
