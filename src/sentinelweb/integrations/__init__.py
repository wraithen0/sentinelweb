"""External tool integrations.

Each integration module is a thin, scope-enforcing wrapper around a
well-known security CLI. The tool itself must be installed separately;
SentinelWeb will report a clear error if it isn't.
"""
